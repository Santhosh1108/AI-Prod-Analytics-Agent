## Sample data

Use the existing synthetic `users.csv` and `events.csv` from the earlier MetricLens project to populate Google Sheets.

Create two tabs:

### users
Columns:
`user_id, platform, acquisition_channel, plan`

### events
Columns:
`user_id, event_name, event_timestamp`

Then deploy the Apps Script as a web app.

